# Dokumente
Dieses Repository enthält die aktuell gültigen Dokumente des Plan b Chor Bonn e.V.

Die PDFs werden bei jedem Push auf `main` automatisch kompiliert und auf plan-b-chor.de veröffentlicht. Der `main`-Branch enthält stets die gültige Fassung aller Dokumente.
